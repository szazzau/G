{
  "note": "This file is generated at runtime if it does not exist. It stores user accounts (username, passwordHash, role).\nDefault admin created on first run: username 'admin', password 'changeme' — change immediately."
}
